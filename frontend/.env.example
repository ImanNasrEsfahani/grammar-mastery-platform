# Server-only absolute Django origin. Do not prefix with NEXT_PUBLIC_.
DJANGO_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Public product locale; supported values are fa and en.
NEXT_PUBLIC_DEFAULT_LOCALE=fa
