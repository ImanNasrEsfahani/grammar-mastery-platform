from django.apps import AppConfig


class Stage21DjangoAdapterConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "backend.django_adapter"
    label = "stage21_django_adapter"
    verbose_name = "Grammar Mastery Stage 21 API Adapter"

