"""Django REST Framework transport adapter for the Stage 21 service layer.

The adapter owns HTTP concerns only. Learning calculations and state
transitions remain in the application/domain services.
"""
