# Stage 21 selected backend runtime: Django 5.2 LTS + Django REST Framework.
Django>=5.2.17,<5.3
djangorestframework>=3.16,<4.0
psycopg>=3.2,<4.0
openpyxl>=3.1,<4.0

# Stage 25 production password policy: Django Argon2PasswordHasher (Argon2id).
argon2-cffi>=23.1,<26.0
